#!/bin/bash

#Homebrew package manager
# /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

# cd /Users/rudy/mysettings/setup
# brew bundle

# sudo visudo
# %admin▸ ▸   ALL = (ALL) NOPASSWD: ALL

# -Install flycut (clipboard manager)
# -Disable Mac OSX default ctrl up, down, left, right keyboard shortcuts
#  to allow these key combinations to be captured by the terminal
# -Add in missing Ctrl + Up and Ctrl + Down keyboard combinations in terminal
# -Terminal to auto close when pressing Ctrl + D
# -Change scroll direction
# -Change click and drag in accessibility
# -Change to use function keys as standard F keys
